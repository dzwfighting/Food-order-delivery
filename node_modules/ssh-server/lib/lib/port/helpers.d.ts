import { ServerConfig } from "../../lib/config";
export interface ForwardPattern {
    localAddress: string;
    localPort: number;
    localStr: string;
    remoteAddress: string;
    remotePort: number;
    remoteStr: string;
}
export declare function parseForwardPattern(config: ServerConfig, payload: string): ForwardPattern;
export declare function genForwardSocket(configDir: string, fw: ForwardPattern): string;
