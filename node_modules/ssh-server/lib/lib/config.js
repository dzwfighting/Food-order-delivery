"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getServerConfig = void 0;
const fs = require("fs");
const yaml = require("js-yaml");
const path = require("path");
function loadServerConfigFile(file) {
    if (!fs.existsSync(file)) {
        return [];
    }
    const config = yaml.load(fs.readFileSync(file, { encoding: "utf-8" }));
    return config.servers;
}
function getServerConfig(configDir, server) {
    const configs = loadServerConfigFile(path.join(configDir, "config.yaml"));
    return configs.find(value => value.name === server);
}
exports.getServerConfig = getServerConfig;
//# sourceMappingURL=config.js.map